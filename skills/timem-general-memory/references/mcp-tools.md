# MCP tools (general scene)

Use **`domain=general`** on search and create.

Full parameter reference: [shared mcp-tools.md](../../shared/mcp-tools.md) (when using this repo) or copy `skills/shared` to `.cursor/skills/shared` alongside installed skills.

Primary tools: `search_memories`, `create_memory`, `delete_memory`, `ready`.

Do not use `should_*` or `begin/end` composite tools.
