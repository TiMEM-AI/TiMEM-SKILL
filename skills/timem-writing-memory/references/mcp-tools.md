# MCP tools (writing scene)

Use **`domain=writing`** on search and create.

Full parameter reference: [shared mcp-tools.md](../../shared/mcp-tools.md) — install `skills/shared` to `.cursor/skills/shared` alongside this skill.

Primary tools: `search_memories`, `create_memory`, `delete_memory`, `ready`.

Atomic MCP memory tools only. See [shared mcp-tools.md](../../shared/mcp-tools.md).
