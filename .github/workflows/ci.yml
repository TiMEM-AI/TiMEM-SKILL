name: Build & Upload Skill Packages
on:
  push:
    branches: [main]
    paths: ['skills/**']
  workflow_dispatch:
concurrency:
  group: skill-ci-${{ github.ref }}
  cancel-in-progress: true
jobs:
  build-and-upload:
    runs-on: runner-debian-13
    env:
      TENCENT_SECRET_ID: ${{ secrets.TENCENT_SECRET_ID }}
      TENCENT_SECRET_KEY: ${{ secrets.TENCENT_SECRET_KEY }}
      TENCENT_COS_BUCKET: ${{ secrets.TENCENT_COS_BUCKET }}
      TENCENT_COS_REGION: ${{ secrets.TENCENT_COS_REGION }}
      COS_PREFIX: timem/skills/standalone
    steps:
      - uses: https://gitee.com/github-actions/checkout@v4
        with:
          github-server-url: https://git.aigility.cloud
          fetch-depth: 1
      # - uses: https://gitee.com/github-actions/setup-python@v4
      #   with:
      #     python-version: '3.11'
      - run: pip install cos-python-sdk-v5
      - run: python scripts/build-all.py
      - name: Package
        run: |
          for d in dist/standalone/*/; do
            skill_id=$(basename "$d")
            (cd dist/standalone && zip -r "${skill_id}.zip" "$skill_id")
          done
      - name: Upload
        run: |
          set -euo pipefail
          [ -n "${TENCENT_SECRET_ID:-}" ] || { echo "SKIP: no COS creds"; exit 0; }
          python3 scripts/upload_cos.py
